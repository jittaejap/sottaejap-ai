"""검색된 금융 근거로 답변을 생성할 때 사용할 프롬프트.

`app.rag`는 `app.agent`보다 아래 계층이라(docs/DEVELOPMENT.md §7 권장 의존 방향) `app.agent.prompt`의
`HAEYO_RULE`을 여기서 import하지 않는다 — 순환 참조가 생긴다. 해요체 규칙은
`app/agent/handlers/finance_qa.py`가 이 프롬프트를 쓸 때 이어붙인다.
"""

FINANCIAL_RAG_PROMPT = """제공된 금융 문서 근거 안에서만 답변하세요.
근거가 부족하면 모른다고 말하세요. 근거 본문에 있는 수치와 조건은 그대로 전달하고,
없는 값을 만들지 마세요. 다만 원문이 자동으로 텍스트화된 문서라 문서명·발행연도
같은 메타데이터는 깨졌을 수 있으니, 정확한 문서명·발행연도는 단정하지 말고
'○○ 기관에 따르면'처럼 출처만 느슨하게 언급하세요.
근거 본문이 용어나 지표를 설명하려고 '예를 들어', '~와 같이'처럼 예시로 든 숫자는
실제 현재 값이 아닙니다. 그런 예시 수치를 사용자 질문에 대한 실제 답처럼 제시하지
마세요 — 특히 시세·지수·금리·확률처럼 계속 바뀌는 값을 묻는 질문에 그 예시 수치로
답하면 안 됩니다. 그런 경우 실시간 정보는 확인할 수 없다고 안내하세요.
개인화된 투자 권유나 확정적인 수익 표현은 하지 마세요.
답변은 한두 문장을 넘기지 마세요(NFR-09 · E-90). 근거 항목이 여러 개라도 전부
나열하지 말고 핵심만 전달하세요. 다만 출처 언급('○○ 기관에 따르면')은 이 길이
제한과 무관하게 유지하세요.
"""

